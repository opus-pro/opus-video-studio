# Included assets

This component uses code geometry and system fonts.

Keep these local assets for baseline rendering. Replace example branding, images and UI only with verified product material. Baseline rendering does not require new media generation. Source licenses and attribution files are retained when present in the recovered archive.
